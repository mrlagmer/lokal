import styled from "styled-components";

import { colors } from "../colors";

export const HrGradient = styled.div`
  padding: 0.03rem;
  background: linear-gradient(to right, ${colors.lg});
  margin: 2rem 0;
`;

export const ExperienceLink = styled.a`
  text-decoration: none;
  color: initial;
`;
